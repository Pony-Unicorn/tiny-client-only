{
  "name": "tiny-client-only",
  "version": "1.0.2",
  "description": "A lightweight React component that renders children only on the client side (SSR safe).",
  "main": "dist/ClientOnly.js",
  "module": "dist/ClientOnly.mjs",
  "types": "dist/ClientOnly.d.ts",
  "files": [
    "dist"
  ],
  "keywords": [
    "react",
    "ssr",
    "nextjs",
    "hydration",
    "client-only",
    "component"
  ],
  "author": "Pony",
  "license": "MIT",
  "peerDependencies": {
    "react": ">=17",
    "react-dom": ">=17"
  },
  "scripts": {
    "build": "tsup src/ClientOnly.tsx --format esm,cjs --dts --clean"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Pony-Unicorn/tiny-client-only.git"
  },
  "devDependencies": {
    "@types/react": "^19.2.2",
    "@types/react-dom": "^19.2.2",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "tsup": "^8.5.0",
    "typescript": "^5.9.3"
  }
}
